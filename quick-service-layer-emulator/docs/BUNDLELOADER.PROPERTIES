# BUNDLELOADER.PROPERTIES
# ==========
# Contained in a bundle file (optional)
# ==========
# 
# If a bundleLoader.properties file is found in a bundle, the file must have one
# entry:

bundleLoader=fully.qualified.classname.of.bundle.loader.that.extends.external.bundle.Loader

# Additionally, a key file called "externalKey" must be present in the root of
# the bundle.
